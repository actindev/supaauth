import React from "react"
import ReactDOM from "react-dom"
import SupaAuthComponent from "./SupaAuthComponent"

ReactDOM.render(
  <React.StrictMode>
    <SupaAuthComponent />
  </React.StrictMode>,
  document.getElementById("root")
)